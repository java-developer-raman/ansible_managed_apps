---

# Ich habe passowrd mit mkpasswd --method=sha-512 Befehl verschlüsselt, und dann hashed string hier gelegt, weil Ansible nur mit Hashed password funkionert.
user_name: raman
user_groups: [sudo,users]
group_name: raman
herunterladen_folder: /tmp

# wichtige Verzeichnis
programs_location: "/home/{{user_name}}/programs"
servers_directory: "{{programs_location}}/servers"
apps_directory: "{{servers_directory}}/apps"

# Java propertiesjdk-8u231.tar.gz
java_home: /usr/lib/jvm/java-8-openjdk-amd64

